name: Windows Build
on:
  push:
    branches: [main]
  pull_request:

jobs:
  windows-check:
    runs-on: windows-latest
    steps:
      - name: Generate CI token
        id: app-token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.CI_TOKEN_PROVIDER_APP_ID }}
          private-key: ${{ secrets.CI_TOKEN_PROVIDER_PRIVATE_KEY }}
          owner: metacraft-labs

      - uses: actions/checkout@v5
        with:
          submodules: recursive

      # `Cargo.toml` declares `codetracer_trace_types` and
      # `codetracer_trace_writer_nim` as path dependencies in
      # ../codetracer-trace-format/. The Linux Lint+Test job clones the
      # sibling explicitly via .github/workflows/ci.yml; do the same here
      # so `cargo check` can resolve them. Pin honours
      # .github/sibling-pins so all platforms agree on the SHA.
      - name: Clone codetracer-trace-format (Cargo path dependency)
        shell: bash
        run: |
          PINS=".github/sibling-pins"
          TF_REF="main"
          if [ -f "${PINS}" ]; then
            PIN="$(grep '^codetracer-trace-format ' "${PINS}" | cut -d' ' -f2)" || true
            [ -n "${PIN}" ] && TF_REF="${PIN}"
          fi
          TOKEN="${{ steps.app-token.outputs.token }}"
          CLONE_URL="https://x-access-token:${TOKEN}@github.com/metacraft-labs/codetracer-trace-format.git"
          target="${GITHUB_WORKSPACE}/../codetracer-trace-format"
          rm -rf "${target}"
          if ! git clone --depth 1 --branch "${TF_REF}" --single-branch \
            "${CLONE_URL}" "${target}" 2>/dev/null; then
            rm -rf "${target}"
            git clone --no-checkout "${CLONE_URL}" "${target}"
            git -C "${target}" checkout "${TF_REF}"
          fi

      - uses: dtolnay/rust-toolchain@stable
      - name: Install capnproto
        shell: pwsh
        run: choco install capnproto -y
      - name: Check
        run: cargo check
      - name: Test
        run: cargo test
